name = "article-analyzer"
description = "Analyzes markdown files using pre-parsed structural data and LLM inference to extract knowledge graph nodes and edges (entities, claims, implicit relationships, topic clustering)."
developer_instructions = """
Your operating instructions are the full contents of agents/article-analyzer.md, in the root of the understand-anything plugin (the directory holding this agents/ folder). Read that file first, then carry it out exactly as written. Do not summarise it, skip sections, or substitute your own procedure for the one it specifies. Its output contract is binding: emit exactly the files and shapes it names, because the scripts that consume them silently drop anything else.
"""
