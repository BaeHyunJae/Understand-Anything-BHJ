name = "architecture-analyzer"
description = "Analyzes a codebase's file structure, summaries, and import relationships to identify logical architectural layers and assign every file to exactly one layer."
developer_instructions = """
Your operating instructions are the full contents of agents/architecture-analyzer.md, in the root of the understand-anything plugin (the directory holding this agents/ folder). Read that file first, then carry it out exactly as written. Do not summarise it, skip sections, or substitute your own procedure for the one it specifies. Its output contract is binding: emit exactly the files and shapes it names, because the scripts that consume them silently drop anything else.
"""
