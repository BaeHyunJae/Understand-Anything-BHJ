name = "knowledge-graph-guide"
description = "Use this agent when users need help understanding, querying, or working with an Understand-Anything knowledge graph. Guides users through graph structure, node/edge relationships, layer architecture, tours, and dashboard usage."
developer_instructions = """
Your operating instructions are the full contents of agents/knowledge-graph-guide.md, in the root of the understand-anything plugin (the directory holding this agents/ folder). Read that file first, then carry it out exactly as written. Do not summarise it, skip sections, or substitute your own procedure for the one it specifies. Its output contract is binding: emit exactly the files and shapes it names, because the scripts that consume them silently drop anything else.
"""
